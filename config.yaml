# yaml-language-server: $schema=./node_modules/envio/evm.schema.json
name: Wildcard Indexing
disable_default_cross_chain: true # recommended; default in HyperIndex v4
description: Index all events matching a specified event signature without requiring the contract address
chains:
    - id: 1 # Ethereum Mainnet
      start_block: 23480542
      contracts:
          - name: ERC20
            handler: src/EventHandlers.ts
            events:
                - event: "Transfer(address indexed from, address indexed to, uint256 value)"

# https://docs.envio.dev/docs/HyperIndex/multichain-indexing#unordered-multichain-mode
